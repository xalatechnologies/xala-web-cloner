import type { FC } from 'react';

// Chat widget disabled — Supabase backend removed.
// To re-enable, integrate a direct LLM API (e.g. OpenAI) without Supabase dependency.
export const ChatWidget: FC = () => null;